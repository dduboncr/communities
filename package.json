{
  "name": "take-home",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm -r run dev",
    "dev:backend": "pnpm -filter backend run dev",
    "dev:frontend": "pnpm -filter frontend run dev",
    "db:init": "docker run -d -p 27017-27019:27017-27019 --name mongodb mongo",
    "db:stop": "docker stop mongodb"
  },
  "devDependencies": {
    "ts-node": "^10.9.2",
    "typescript": "^5.2.2"
  },
  "engines": {
    "node": "^20.0.0"
  },
  "dependencies": {
    "react-router-dom": "^6.23.1"
  }
}
